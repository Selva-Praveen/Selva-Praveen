def plusMinus(arr,n):
    # Write your code here
    plus = sum(1 for item in arr if item > 0)
    minus = sum(1 for item in arr if item < 0)
    zero = arr.count(0)
    print(plus/n,minus/n,zero/n,sep='\n')
